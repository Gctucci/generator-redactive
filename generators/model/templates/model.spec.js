'use strict';

var chai = require('chai');
var should = chai.should();
var models = require('../');

describe("<%= modelName.charAt(0).toUpperCase() + modelName.slice(1); %> Model", function(){
  it('should store the model correctly in the database', function(done){
  })
  it('should not store the model if a required field is not provided', function(done){
  })

})
